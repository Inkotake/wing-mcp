# /create-issue-plan

Create a development plan from an issue.

Usage: `/create-issue-plan <issue-file>`

What this does:
1. Reads the specified issue file
2. Creates an implementation plan with tasks
3. Identifies dependencies and risks
4. Breaks down into clear implementation steps

The resulting plan is written to the plans directory for approval.
