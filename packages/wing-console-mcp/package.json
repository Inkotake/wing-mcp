{
  "name": "wing-console-mcp",
  "version": "0.1.0",
  "description": "Behringer WING Family Console MCP Server",
  "type": "module",
  "main": "./dist/server.js",
  "bin": {
    "wing-console-mcp": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc && node --input-type=module -e \"import{cp,mkdir}from'node:fs/promises';await mkdir('dist/schema',{recursive:true});await cp('src/schema/propmap.jsonl','dist/schema/propmap.jsonl')\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
